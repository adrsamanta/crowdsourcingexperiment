/**
 * Created by alan on 10/1/2016.
 */
if (configInfo.count == 0) {
    configInfo.insert({
        name: "numUserGroups",
        value: 6
    });

    configInfo.insert({
        name: "numPlayersPerGroup",
        value: 5
    });
}